# Copy to .env and fill in. Get a key at https://openrouter.ai/keys
OPENROUTER_API_KEY=sk-or-v1-...

# Any OpenRouter chat model id. Measured on this project's test set (scripts/evaluate.py):
#   minimax/minimax-m3:free      free, ~2 s per answer, good JSON discipline   <- default
#   google/gemini-2.5-flash      paid but a few cents for the whole test set, ~1.7 s
# Avoid the "openrouter/free" router: it can hand a question to a safety classifier.
# Free models rate-limit; the client retries with backoff. Use scripts/probe_models.py to compare.
OPENROUTER_MODEL=minimax/minimax-m3:free

# Local embedding model (downloaded once by sentence-transformers, ~130 MB)
EMBEDDING_MODEL=BAAI/bge-small-en-v1.5

# Passages returned with every answer
TOP_K=6

# Best-passage cosine below which the corpus is treated as silent without calling the LLM
MIN_SIMILARITY=0.45
